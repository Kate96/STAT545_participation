#' @title Apply corresponding power to the input
#' @param x Vector of numerics
#' @return The vector x, with corresponding power.
#' @rdname common_doc
#' @export
square <- function(x) pow(x, 2)
# run document() function to process the tags
# and then install&restart on the build tab
